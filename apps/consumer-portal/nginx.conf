server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Read-only path-proxy to site-service (same-origin).
    location /site/ { proxy_pass http://site-service:8000/api/v1/; }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
