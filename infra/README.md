# infra

Deployment for Consmat AI V1. **Not yet built.** Will hold `docker-compose.yml`, Dockerfiles, and DB
migration/seed scripts as the backend and apps are implemented. Target landscape: backend + PostgreSQL +
three nginx-served frontends. See [`../docs/SLD.md`](../docs/SLD.md).
