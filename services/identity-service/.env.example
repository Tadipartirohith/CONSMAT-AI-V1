# identity-service configuration
DATABASE_URL=postgresql+psycopg://consmat:consmat@localhost:5432/identity
API_PREFIX=/api/v1
# MUST match every other service's JWT_SECRET so tokens validate everywhere.
JWT_SECRET=change-me-in-prod
JWT_ALG=HS256
ACCESS_TOKEN_TTL_MIN=1440
DEMO_PASSWORD=consmat123
