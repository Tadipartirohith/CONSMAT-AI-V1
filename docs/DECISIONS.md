# Consmat AI V1 — Decision Log & Open Questions

A lightweight ADR-style log. **Confirmed** decisions are locked; **Open** items are resolved as we
design each roadmap step. Date format: YYYY-MM-DD.

---

## Confirmed decisions

| ID | Date | Decision | Rationale |
|----|------|----------|-----------|
| **D1** | 2026-08-16 | **Hub owns inventory & resells; hub sets price; hub is also a supplier and procures from external vendors** | Hub is the merchant of record and distribution authority (distributor model) |
| **D2** | 2026-08-16 | **Topology: single hub → many spokes** | Simplest realistic v1; generalizable later |
| **D3** | 2026-08-16 | **Spoke holds no stock; material ships hub → site; only the hub tracks inventory** | Spoke is a coordination/relationship role (a person), not a warehouse |
| **D4** | 2026-08-16 | **9-phase construction model drives phased JIT demand** | Precise just-in-time dispatch; matches site progression |
| **D5** | 2026-08-16 | **4-tier consumer classification: Individual · Contractor · Commercial · Government/Institutional** | Supports differentiated pricing/credit/priority |
| **D6** | 2026-08-16 | **Hub LLM for procurement intelligence** (profitability, alternatives, price-scouting) | Moves V0's LLM upstream to buying/pricing decisions |
| **D7** | 2026-08-16 | **Persistent database (PostgreSQL assumed) instead of in-memory** | Inventory ledger requires durability & auditability |
| **D8** | 2026-08-16 | **Microservices architecture** (one service per domain) | Resolves Q4; chosen over modular monolith for independent scaling/deploy |
| **D9** | 2026-08-16 | **Persistence stack: SQLAlchemy 2.0 + Alembic on PostgreSQL** | Resolves Q5; battle-tested ORM + migrations, transactional ledger |
| **D10** | 2026-08-16 | **Microservices conventions**: database-per-service, synchronous REST to start (events later), per-service FastAPI + Dockerfile + Alembic, API gateway deferred | Standard microservice defaults; keeps services independent |
| **D11** | 2026-08-16 | **Payments are config-driven**: provider (mock default) + each provider's API base URL and secret **env-var names** live in `payment-service/config.yaml`; secret values only in env. Real providers are extension points. | Swap gateways without code changes; keep keys out of the repo |
| **D12** | 2026-08-16 | **Hub LLM provider set to Gemini** via `infra/.env` (`AI_PROVIDER=gemini`, `AI_MODEL=gemini-flash-lite-latest`); goes live once an API key is added to `AI_API_KEY` | Enable procurement intelligence; key supplied by the operator, never committed |
| **D13** | 2026-08-16 | **API gateway built** (top-level `gateway/`, nginx): single ingress fronting all services under `/api/<service>/` with central CORS. All frontends repointed to route API traffic through it. Supersedes the "deferred" note in D10. | One API entry point + a home for cross-cutting concerns |
| **D17** | 2026-08-16 | **Live web-grounded price-scout + auto-scout in analyze.** `SCOUT_PROVIDER=auto` now prefers `web` (Gemini **Google-Search grounding** via native `generateContent`, real cited source URLs) when a Gemini key is set, degrading to `llm` estimates (no fabricated URLs) then `stub`. `/procurement/analyze` **auto-scouts** every demanded material (cached; `?refresh=1` forces) so the Hub LLM always has external offers, and the prompt now **requires** flagging a materially-cheaper source even when the registry/inventory already supplies it (per-unit + line saving, indicative-to-verify). Extends D16. NB: live grounding needs a Gemini plan with Search-grounding quota; the free-tier key falls back to estimates. | User: "LLM should search the internet and give cheaper alternatives even if stock is present" |
| **D16** | 2026-08-16 | **External price sources (price-scout).** procurement gains `external_offers` + a pluggable scout (`SCOUT_PROVIDER=auto/llm/stub`): the Hub LLM returns **indicative** internet prices (IndiaMART/TradeIndia style), and supplier price lists import as **firm** offers (`/external-offers/import`). External offers are **advisory** — the deterministic plan still buys from the registry; the LLM factors them into advice ("onboard cheaper supplier"). **No IndiaMART scraper** (no clean price API + ToS/anti-bot); real web-search (SerpAPI/Bing/Gemini-grounding) is a documented extension point. | Market intelligence without a fragile scraping foundation |
| **D15** | 2026-08-16 | **Procurement is tier-agnostic.** Buying doesn't depend on the consumer tier; `/procurement/analyze` no longer takes `tier`. Profitability is a reference lens computed against the hub **list price** (pricing-service, no tier), or an explicit `selling_prices` override. Removes the earlier tier smell. | Buying and selling are separate concerns |
| **D14** | 2026-08-16 | **Product/brand layer + full-name search.** Catalog (inventory) gains `products` (branded SKUs under a material); vendor pricing + procurement plans move to the product level (several companies per material); `GET /products/search?q=` does full product-name search. BOM and hub stock stay at material level *(superseded by D18/D19 — stock + BOM move to product level)*. | Real procurement is brand-specific; buyers search by full product name |
| **D18** | 2026-08-17 | **Product/brand-level stock + 3x buffer.** inventory gains `ProductStock` (on_hand/reserved/avg_cost per branded product) as the source of truth; the material `InventoryItem` is kept as a rollup (maintained on every product movement) so material views still work. `low_stock` flags any product whose `on_hand < 3x reserved` (the hub's committed demand). Procurement receipts and site dispatch now move stock at the product level; `reserve_product(allow_over=True)` lets committed demand exceed on-hand so the buffer surfaces low/no-stock early. Supersedes the "stock stays material-level" half of D14. | User: "track stock and price per brand… hub needs 3x the stock of all requests" |
| **D19** | 2026-08-17 | **CE/spoke-entered product BOM + phase dates + approval.** The Bill of Materials is entered (and edited before start) by the civil engineer or spoke at the product level (`POST /sites/{id}/bom`), reserved against hub stock, and sliced into the 9 phases by the material weight matrix at dispatch time (replaces the architect auto-plan as the primary path; auto-plan kept as legacy). Each phase carries a planned **start + end** date; a civil engineer's change to an end date becomes a **pending `PhaseDateChange`** needing spoke or manager approval, while a spoke/manager edit applies directly. Resolves Q1 (dates) & Q6. | User: "civil engineer or spoke enters the BOM… CE change needs spoke/manager approval; manager sits above the spoke" |
| **D20** | 2026-08-17 | **Background JIT scheduler + notifications.** site-service runs a background thread that, ~3 days before a phase's planned end, notifies the field team and **pre-dispatches** the next phase's materials (~1 day out) so work is never halted. Idempotent via a per-phase `dispatched` flag. `Notification` records feed in-app feeds (hub-console Projects, spoke SiteDetail); no email in v1. `POST /scheduler/tick` runs one pass on demand. | User: "3 days before a phase ends, notify CE + spoke that next-phase stock dispatches in a day" |
| **D21** | 2026-08-17 | **Vendor add/remove request → approval.** New `hub_ops` operator role can *request* a vendor add or remove (`POST /vendor-requests`); only `hub_supervisor`/`hub_manager` may `…/decide` (approve creates/deactivates the vendor). Supervisor/manager keep direct add/remove. Resolves Q1/Q8 for vendors. | User: "ops can request add/remove; supervisor or manager approves" |
| **D22** | 2026-08-17 | **Per-product (brand) margins.** pricing `MarginRule` gains `product_id`; precedence is now product+tier > product > material+tier > material > tier > global > default. `GET /price-product/{id}` prices a product at its own brand landed cost. Extends Q3. | User: "margin should be set per product, not per category" |
| **D23** | 2026-08-22 | **Delivery confirmation is a field/hub action, not the customer's.** A dispatch is confirmed received by the CE/spoke (or hub staff) via `POST /dispatches/{id}/confirm` (role-guarded); the consumer portal shows delivery status read-only. The scheduler nudges the field with a `confirm_reminder` when a delivery stays unconfirmed. Reverses the earlier customer-confirms design. | User: "customer will not confirm any deliverables — CE/Spoke confirms the delivery" |
| **D24** | 2026-08-22 | **Audience-scoped events across all three apps.** `Notification.audience` (`field` / `all` / `consumer`) drives who sees each event; the consumer portal shows an updates feed + timeline scoped to their own project (audience `all`/`consumer`), and the hub-console gains a **Network events** feed (all events, filterable by kind). | User: "events and notifications are missing in the hub" |
| **D25** | 2026-08-22 | **RBAC managed through a Team portal + role hierarchy.** identity gains `ROLE_RANK`/`can_manage` (admin manages everyone incl. other admins; each role manages only strictly-lower ranks), `GET /roles` (actor's assignable set), and `PATCH /users/{email}` (assign role, move to a spoke team via `org_ref`, (de)activate). The hub-console **Team & access** page (admin/manager/supervisor only) adds spokes + regions, creates members below the actor's rank, and reassigns/deactivates. Multiple admins supported; self-deactivation blocked. | User: "create a Team management portal, modify RBAC through the UI… admins set roles for supervisors/admins, supervisors set roles for ops" |
| **D26** | 2026-08-22 | **Hub-console UX conventions.** Plain ASCII only (no `—`/`→`). Page drill-down + filters live in URL search params, so a left-nav click always returns to the section home and widgets can deep-link (e.g. Overview donut → `/projects?health=red`). Overview shows a **project-health donut** (hover = count, click = filtered list) and a **stock-buffer watch** widget (out/low/watch vs the 3× reserved buffer). Inventory clamps displayed `available` to ≥ 0 (flagging over-committed in red) and offers **Procure** per product (compare hub vendor rates + live open-market rates, then raise a PO). Still-short re-dispatch shows in red. | User test feedback batch (2026-08-22) |
| **D27** | 2026-08-22 | **LLM market alternatives for unavailable items + architect role defined.** The Hub LLM now surfaces open-market substitutes for demanded items with no registry vendor (commodity brands are interchangeable; specialty boundaries preserved), scouting by the demanded product name. The **architect** is the design authority on a site: owns the Bill of Materials (products & quantities) and the phase schedule; the civil engineer executes phases; the spokesperson owns coverage — surfaced as a per-role guide in the spoke-app. | User: "LLM isn't suggesting market products for unavailable items (JSW Neosteel)… not sure what the architect updates — update his sites accordingly" |
| **D28** | 2026-08-22 | **Customer NBFC flag at onboarding.** A consumer carries `is_nbfc`, captured at the spoke onboarding form and cascaded to the admin (hub-console Projects rows + spoke customer list) so the hub knows which customers are NBFC-financed. Financing/credit itself is out of scope (V1 is a distributor, not the lender). | User: "when a customer is onboarding, add a column at spoke layer if it is NBFC or not and cascade the info towards admin" |
| **D29** | 2026-08-22 | **Escrow release on delivery confirmation.** Project payments are captured but HELD in escrow; as deliveries are confirmed at the site, site-service releases the delivered fraction (received dispatches / planned phases) of the held amount via payment-service (POST /payments/release), flipping the payment to `released` at 100%. Release is best-effort - a payment outage never blocks a confirmation. Mirrors consmat.in "funds released only upon successful delivery". | User: "implement escrow, funds held until delivery confirmed" |
| **D30** | 2026-08-22 | **Catalog breadth.** The seed grows from 5 base materials to 20 categories / ~125 product lines / ~55 brands (adds RMC, blocks, formwork, MS/GI pipes, plumbing & sanitaryware, electrical, wires, tiles, paints, waterproofing, adhesives, plywood, doors, glass, hardware). New categories carry per_sqft=0 and dispatch via an explicit BOM phase, so the structural auto-plan (cement/steel/sand/aggregate/bricks) is unchanged. | User: "catalog breadth - real product is 33+ categories / 200+ brands" |
| **D31** | 2026-08-24 | **Spoke team + Finance role.** A spoke runs a small area with a team: multiple architects, civil engineers, and a new spoke-scoped **finance** role (identity ROLES/ROLE_RANK 40, managed in the Team portal via org_ref = spoke id). Extends D25. | User: "the spoke will have architect(s), civil engineer(s), Finance(s) under him" |
| **D32** | 2026-08-24 | **Per-project captive/client type + lifecycle stage.** Each project (site) carries `project_type` (captive|client, chosen at/after onboarding, replaces the consumer NBFC flag from D28) and a `stage` machine (onboarded -> design_uploaded -> boq_review -> boq_approved -> budgeted -> financing/finance_approved | awaiting_payment/paid -> scheduling -> active -> completed), distinct from the construction `status`. | User: "in place of NBFC, captive or client for every project" |
| **D33** | 2026-08-24 | **Project design document store.** The architect uploads a design (CAD/pdf) per project (`ProjectDocument`, binary in DB, 8MB cap, object store is a later extension point); the CE builds the BOQ from it. | User: "architect will upload a design (CAD mostly)" |
| **D34** | 2026-08-24 | **Dual BOQ + 5% reconciliation + two-gate approval + hub change loop.** The CE BOQ is cross-checked against a second BOQ from an external app (pluggable estimator in procurement, `BOQ_PROVIDER=stub|external`); if the worst per-product resource gap exceeds 5% a reconciled **final BOQ** is required. The final BOQ needs **spoke + hub** approval (both gates); on full approval it becomes the operational BOM (reserved against hub stock) and a stock check flags low/out-of-stock. A hub-requested change needs **spoke + CE** acknowledgement, which reopens the BOQ. All steps emit events. | User: "external app BOQ, >5% diff pop-up + final BOQ; spoke+hub approval; hub changes need spoke+CE approval; inform low/out of stock" |
| **D35** | 2026-08-24 | **Budget engine + finance module.** Once the BOQ is approved the hub issues a **budget** priced from the final BOQ at the consumer tier (pricing `/quote-products`). The internal finance team works a per-project `ProjectFinance` record (status pending->in_progress->approved/rejected, partner, amount, remarks) against a **preferred finance-partners** registry. | User: "budget from hub; internal finance team; preferred finance partners" |
| **D36** | 2026-08-24 | **Phase-date <1-week escalation + delivery triggers.** A CE end-date change that leaves the next phase starting under 7 days away is **escalated**: it requires a remark and hub approval (not the spoke alone). Deliveries are gated per project type: **captive** starts once finance is approved AND every phase has start+end dates; **client** starts the moment a payment is received. The gate lives in the single dispatch funnel (`_ensure_phase_dispatched`) + `start_site`. | User: "captive: finance approved -> phase-wise delivery after CE sets dates; <1 week change needs remarks + approval; client: on payment received trigger deliveries" |
| **D37** | 2026-08-24 | **Material classification into ConSmat page-3 verticals.** `Material.segment` groups the catalog into the six business verticals - S&F (Structure & Foundation), B&B (Bricks & Blocks), S&S (Sheets & Shades), P&P (Pipes & Plugs), MixG&FixG (Mortars/Adhesives/Coatings), Interiors & Home - and the catalog is expanded to the full page-3 taxonomy (40 categories). Inventory + the market dashboard group by segment. | User: "classify all the materials as per the attached pdf page 3" |
| **D38** | 2026-08-24 | **Spoke stock-order request -> hub approval.** A spoke raises an `OrderRequest` (products + qty, optional project ref + note) that goes to the hub; the hub approves by choosing a vendor + per-product rate, which auto-creates a real purchase order (linked `order_id`), or rejects. | User: "when spoke needs to order something then hub needs to approve it so the request goes to hub" |
| **D39** | 2026-08-24 | **Market watch + price outlook on the hub dashboard.** procurement stores a daily `MarketSnapshot` per material (avg of scouted offers / vendor registry) and `GET /market/index` returns per-material day-over-day movement + an up/down/stable trend (synthesizing a recent series until real daily snapshots accumulate). The hub Overview shows a per-segment **Market watch** (daily movement + sparkline) and a **Price outlook** (will materials rise/fall per vertical). | User: "live update of steel and other segments per day + an estimation whether materials go up or down per open market" |
| **D40** | 2026-08-29 | **Rename civil engineer -> site engineer.** The role id is `site_engineer` system-wide (identity ROLES/ROLE_RANK, all `require_role` sets, all UIs), the demo login is `site@consmat.com`, and every label reads "Site Engineer"; identity migration 0002 renames existing rows. | User: "replace everything with site engineer wherever civil engineer is present" |
| **D41** | 2026-08-29 | **Fund type chosen at onboarding.** `Consumer.fund_type` (captive|client) is captured on the onboarding form; each new project defaults its `project_type` from it (still overridable per project). The site-create form drops the selector. | User: "during onboarding, add fund type here instead of site" |
| **D42** | 2026-08-29 | **Design upload is architect-only.** `POST /sites/{id}/documents` requires the architect role; the site engineer and spoke can view + download the design to build the BOQ but cannot upload it. | User: "why is design files in ce workspace" + "SE views/downloads; only architect uploads" |
| **D43** | 2026-08-29 | **Spoke location defaults + order project dropdown.** Location fields (onboarding, new site) default to the spoke's assigned region, becoming a dropdown when the spoke covers more than one; the stock-order request `For project` is a dropdown of the spoke's sites (optional). Product stock everywhere is the hub inventory. | User: "default the spoke location; dropdown if >1; optionally mention site details" |
| **D44** | 2026-08-29 | **Financier eligibility + auto-updating finance dashboard.** `ProjectFinance.eligibility` (pending|review|eligible|not_eligible) is set by the finance team (migration 0016). Design/budget/BOQ changes on a captive project auto-fire `finance`-audience notifications (`_notify_finance`) so the financier is kept in sync. A finance login exists per spoke. | User: "financier gets details, updates eligibility; modifications update his dashboard automatically" |
| **D45** | 2026-08-29 | **Advanced end-to-end notifications + WhatsApp.** Comprehensive in-app feeds scoped by audience (field/all/consumer/finance) with unread + mark-all-read: a spoke Notifications page (finance sees the finance feed) and the customer updates feed. Customer-facing events also dispatch over **WhatsApp** via a pluggable `whatsapp_client` (stub by default; Meta WhatsApp Cloud API when `WHATSAPP_*` is set). | User: "complete notifications for spoke + customer + WhatsApp integration for customer side" |

### The 9 phases (per D4)
1. Excavation & footing
2. Foundation & plinth beam
3. RCC superstructure (repeats per floor)
4. Masonry / brickwork
5. Roofing / terrace slab
6. Internal plastering
7. External plastering
8. Flooring & tiling
9. MEP & finishing

---

## Open questions

| ID | Question | Affects | Target step |
|----|----------|---------|-------------|
| ~~Q1~~ | ~~Manager vs supervisor approvals~~ → **Partly resolved: vendor add/remove needs supervisor/manager approval (D21); civil-engineer phase end-date changes need spoke/manager approval (D19). Value thresholds still open.** | Roles/permissions | 🟡 |
| ~~Q8~~ | ~~Procurement approval workflow~~ → **Resolved for vendors: request (hub_ops) → approve (supervisor/manager), D21. Purchase-order approval thresholds still open.** | Procurement | 🟡 |
| Q2 | Exact phase→material weight matrix — **default set in site-service `bom.py` (sums to 1.0/material); still tunable.** Per-floor RCC repetition modelled at building level for now. | BOM/JIT | 🟡 default set |
| ~~Q3~~ | ~~Margin model~~ → **Resolved: one margin-rule table with precedence (material+tier > material > tier > global > service-default). Supports flat, per-tier, and per-material at once.** | Pricing | ✅ |
| ~~Q4~~ | ~~Backend shape~~ → **Resolved: microservices (D8)** | Architecture | ✅ |
| ~~Q5~~ | ~~DB & migrations~~ → **Resolved: SQLAlchemy 2.0 + Alembic + Postgres (D9)** | Persistence | ✅ |
| ~~Q11~~ | ~~Material/catalog ownership~~ → **Resolved: inventory-service owns the catalog and exposes `GET /materials`; other services reference `material_id` as an opaque string (no cross-service FK). Extract a catalog-service later if it grows.** | Architecture | ✅ |
| ~~Q12~~ | ~~Inter-service auth~~ → **Resolved: BOTH — identity-service issues JWTs; every service validates locally with the shared JWT_SECRET (HS256); internal service-to-service calls use a minted `service` token.** Frontends log in and attach the token; role guards per endpoint. | Security | ✅ |
| Q6 | Is the consumer portal in V1 scope, or spoke-mediated only at first? | Frontends | Step 8 |
| ~~Q7~~ | ~~Site geolocation & spoke geofencing~~ → **Resolved: area-keyword matching (spoke covers keywords; location served by the spoke whose keyword appears, most-specific wins). Lat/lng deferred.** | Spoke ops | ✅ |
| Q9 | Materials catalog beyond the 5 base items (tiles, cement types, MEP) | Catalog | later |
| Q10 | Credit terms per consumer tier | Pricing/finance | later |

---

## Change history
- **2026-08-16** — Initial pivot from V0 buyer-centric marketplace to V1 hub-and-spoke; D1–D7 confirmed.
- **2026-08-16** — Built through the gateway (D13); product/brand layer + price-scout (D14–D17).
- **2026-08-17** — Site-dispatch system: product-level stock + 3x buffer (D18), CE/spoke product BOM + phase dates + approval (D19), background JIT scheduler + notifications (D20); vendor request→approval + `hub_ops` role (D21); per-product margins (D22).
- **2026-08-22** — Confirm-by-field delivery + reminder (D23); audience-scoped events incl. hub Network-events feed (D24); RBAC Team portal + role hierarchy (D25); hub-console UX pass — plain ASCII, URL-driven nav/filters, health donut + stock-buffer widget, inventory available-clamp + procure-from-inventory (D26); LLM open-market alternatives for unavailable items + architect role defined (D27). NBFC onboarding flag (D28); escrow release on delivery confirmation (D29); catalog breadth to 20 categories/~125 lines (D30).
- **2026-08-24** - Spoke project lifecycle epic: team + finance role (D31); per-project captive/client + stage machine (D32); design document store (D33); dual-BOQ 5% reconciliation + spoke/hub approval + hub change loop + stock check (D34); budget engine + finance module + partners (D35); phase-date <1-week escalation + captive/client delivery triggers (D36).
- **2026-08-24** - Catalog classified into ConSmat page-3 verticals + full taxonomy (D37); spoke stock-order request -> hub approval -> PO (D38); daily market snapshots + hub Market-watch / Price-outlook widgets (D39).
- **2026-08-29** - Civil engineer -> Site engineer rename (D40); fund type at onboarding (D41); design upload architect-only (D42); spoke location defaults + order project dropdown (D43); financier eligibility + auto-updating finance notifications (D44); advanced in-app notifications + WhatsApp customer integration (D45).
